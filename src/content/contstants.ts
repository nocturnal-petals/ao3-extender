export enum Colors {
   READ = '--color-read',
   UPDATED = '--color-updated',
   PARTIAL = '--color-partial',
   DO_NOT_READ = '--color-do-not-read',
   DOWNLOADED = '--color-downloaded',
   KUDOS = '--color-kudos',
   SEEN = '--color-seen',
   READING = '--color-reading',
   ON_HOLD = '--color-on-hold',
   SHADOW = '--color-shadow',
   AO3_RED = '--color-ao3-red',
}